The follwoing files have been recently edited in the `data/templates/diverse` directory and need to be copied over to the project directories to fix issues.

* Baseline.rb --> copy into diverse and coincident directories for all activities. It is really only needed for diverse, but may as well do them all. This addresses the scheduling shifting error that occurs due to different version of OpenStudio Standards.
